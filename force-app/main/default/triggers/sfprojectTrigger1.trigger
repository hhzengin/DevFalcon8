
trigger salesforceProject on Salesforce_Project__c (before insert, after insert, before update, after update) {
   /*system.debug('.....TRIGGER START.....');
    list<Salesforce_Project__c> ProList = trigger.new;
    if(trigger.isAfter){
    //project name displayed
        system.debug('After trigger trigger.new: ' + trigger.new);
    }
    if(trigger.isUpdate){
    //project name 
        system.debug('Update trigger.new: ' + trigger.new);
    }   
      system.debug('.....TRIGGER END.....'); */
}

