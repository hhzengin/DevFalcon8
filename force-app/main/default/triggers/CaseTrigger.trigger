trigger CaseTrigger on Case (before insert,after insert, before update, after update, before delete, after delete) {
   /*
    if(trigger.isBefore){
        system.debug('.......Trigger is BEFORE........' + trigger.isBefore);
        if(trigger.isInsert){
        system.debug('Insert BEFORE' + trigger.isInsert);
        }
        if(trigger.isUpdate){
            system.debug('Update BEFORE' + trigger.isUpdate);
        }        
    }
    if(trigger.isAfter){
        system.debug('.......Trigger is AFTER........' + trigger.isAfter);
        if(trigger.isInsert){
        system.debug('Insert AFTER ' + trigger.isInsert);
        }
        if(trigger.isUpdate){
            system.debug('Update AFTER ' + trigger.isUpdate);
            system.debug('Delete AFTER ' + trigger.isUpdate);
        } 
        if(trigger.isDelete){
            system.debug('Delete BEFORE ' + trigger.isDelete);
        }         
    }  
*/
}