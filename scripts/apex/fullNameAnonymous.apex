string firstName = 'Hande';
string lastName = 'Zengin';
string fullName = firstName + ' ' + lastName;
system.debug(fullName);

//null. We are only declaring here
string declaredSTR;
SYSTEM.DEBUG(declaredSTR);

//empty
string str2 = '';
